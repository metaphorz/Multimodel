# Python
venv/
__pycache__/
*.pyc

# Large regenerable data (rebuild via pipeline/fetch_nlcd.sh)
data/nlcd_grid.tif

# Large regenerable data (rebuild via pipeline/fetch_cadastral.sh)
data/Cadastral_Statewide.zip
data/cadastral/

# Test/server artifacts
tests/auto/*.png
tests/auto/server.log
tests/auto/server.pid
tests/auto/downloads/

# OS / LaTeX build cruft
.DS_Store
*.aux
*.log
*.out
*.toc
*.synctex.gz
docs/*.aux
docs/*.log
docs/*.out

# LaTeX build artifacts
docs/*.fls
docs/*.fdb_latexmk
docs/build.log

# Local email correspondence (never commit)
*.eml

# Dynamic-Powell animation frames: 18 MB of generated uint8 fields (300 storms).
# Committed deliberately once the precompute completes, NOT while it is mid-run --
# a partial set would ship a viewer that 404s on half the storms.
